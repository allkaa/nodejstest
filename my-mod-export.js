'use strict';
// Exported modules are in strict mode whether you declare them as such or not.
// The export statement cannot be used in embedded scripts.

// simple CommonJS module
module.exports = (param) => {return console.log(param)}

